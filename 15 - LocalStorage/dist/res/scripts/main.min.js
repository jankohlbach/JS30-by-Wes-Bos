const addItems=document.querySelector(".add-items"),itemsList=document.querySelector(".plates"),items=JSON.parse(localStorage.getItem("items"))||[],populateList=(plates=[],platesList)=>{platesList.innerHTML=plates.map((plate,i)=>`\n      <li>\n        <input type="checkbox" data-index=${i} id="item${i}" ${plate.done?"checked":""} />\n        <label for="item${i}">${plate.text}</label>\n      </li>\n    `).join("")},addItem=e=>{e.preventDefault();const form=e.target,text=form.querySelector("[name=item]").value,item={text:text,done:!1};items.push(item),populateList(items,itemsList),localStorage.setItem("items",JSON.stringify(items)),form.reset()},toggleDone=e=>{if(!e.target.matches("input"))return;const element=e.target,{index:index}=element.dataset;items[index].done=!items[index].done,localStorage.setItem("items",JSON.stringify(items)),populateList(items,itemsList)};addItems.addEventListener("submit",addItem),populateList(items,itemsList),itemsList.addEventListener("click",toggleDone);
//# sourceMappingURL=main.min.js.map