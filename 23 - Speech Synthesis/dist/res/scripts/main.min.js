const msg=new SpeechSynthesisUtterance;let voices=[];const voicesDropdown=document.querySelector('[name="voice"]'),options=document.querySelectorAll('[type="range"], [name="text"]'),speakButton=document.querySelector("#speak"),stopButton=document.querySelector("#stop");msg.text=document.querySelector('[name="text"]').value;const populateVoices=()=>{voices=speechSynthesis.getVoices(),voicesDropdown.innerHTML=voices.map(voice=>`<option value="${voice.name}">${voice.name} (${voice.lang})</option>`).join("")};function toggle(startOver=!0){speechSynthesis.cancel(),startOver&&speechSynthesis.speak(msg)}function setVoice(){msg.voice=voices.find(voice=>voice.name===this.value),toggle()}function setOption(){msg[this.name]=this.value,toggle()}speechSynthesis.addEventListener("voiceschanged",populateVoices),voicesDropdown.addEventListener("change",setVoice),options.forEach(option=>option.addEventListener("change",setOption)),speakButton.addEventListener("click",toggle),stopButton.addEventListener("click",()=>toggle(!1));
//# sourceMappingURL=main.min.js.map