function playSound(event){const audio=document.querySelector(`audio[data-key="${event.keyCode}"]`),key=document.querySelector(`.key[data-key="${event.keyCode}"]`);audio.currentTime=0,audio.play(),key.classList.add("playing")}function removeTransition(event){"transform"===event.propertyName&&this.classList.remove("playing")}const keys=document.querySelectorAll(".key");keys.forEach(function(key){key.addEventListener("transitionend",removeTransition)}),window.addEventListener("keydown",playSound);