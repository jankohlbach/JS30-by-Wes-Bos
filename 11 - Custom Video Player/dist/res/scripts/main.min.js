const player=document.querySelector(".player"),video=player.querySelector(".viewer"),progress=player.querySelector(".progress"),progressBar=player.querySelector(".progress-filled"),toggle=player.querySelector(".toggle"),skipButtons=player.querySelectorAll("[data-skip]"),ranges=player.querySelectorAll(".player-slider"),togglePlay=()=>{video.paused?video.play():video.pause()},updateButton=()=>{const icon=video.paused?"►":"❚ ❚";toggle.textContent=icon},skip=e=>{video.currentTime+=parseFloat(e.target.dataset.skip)},handleRangeUpdate=e=>{video[e.target.name]=e.target.value},handleProgress=()=>{const percent=video.currentTime/video.duration*100;progressBar.style.flexBasis=`${percent}%`},scrub=e=>{const scrubTime=e.offsetX/progress.offsetWidth*video.duration;video.currentTime=scrubTime};video.addEventListener("click",togglePlay),video.addEventListener("play",updateButton),video.addEventListener("pause",updateButton),video.addEventListener("timeupdate",handleProgress),toggle.addEventListener("click",togglePlay),skipButtons.forEach(button=>button.addEventListener("click",skip)),ranges.forEach(range=>range.addEventListener("click",handleRangeUpdate));let mousedown=!1;progress.addEventListener("mousedown",()=>{mousedown=!0}),progress.addEventListener("mouseup",()=>{mousedown=!1}),progress.addEventListener("click",scrub),progress.addEventListener("mousemove",e=>mousedown&&scrub(e));
//# sourceMappingURL=main.min.js.map