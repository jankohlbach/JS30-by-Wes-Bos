const holes=document.querySelectorAll(".hole"),scoreBoard=document.querySelector(".score"),moles=document.querySelectorAll(".mole");let lastHole,timeUp=!1,score=0;function randomTime(min,max){return Math.round(Math.random()*(max-min)+min)}function randomHole(){const idx=Math.floor(Math.random()*holes.length),hole=holes[idx];return hole===lastHole?randomHole(holes):(lastHole=hole,hole)}function peep(){const time=randomTime(300,1e3),hole=randomHole(holes);hole.classList.add("up"),setTimeout(()=>{hole.classList.remove("up"),timeUp||peep()},time)}function startGame(){scoreBoard.textContent=0,timeUp=!1,score=0,peep(),setTimeout(()=>{timeUp=!0},1e4)}function bonk(e){e.isTrusted&&(score+=1,this.classList.remove("up"),scoreBoard.textContent=score)}moles.forEach(mole=>mole.addEventListener("click",bonk));
//# sourceMappingURL=main.min.js.map