const endpoint="https://gist.githubusercontent.com/Miserlou/c5cd8364bf9b2420bb29/raw/2bf258763cdddd704f8ffd3ea9a3e81d25e2c6f6/cities.json",cities=[],searchInput=document.querySelector(".search"),suggestions=document.querySelector(".suggestions"),getData=async()=>{try{const response=await fetch(endpoint);if(response.ok){const jsonResponse=await response.json();cities.push(...jsonResponse)}}catch(error){console.log(error)}};getData();const findMatches=(match,array)=>array.filter(place=>{const regex=new RegExp(match,"gi");return place.city.match(regex)||place.state.match(regex)}),displayMatches=e=>{const input=e.target.value,matchArray=findMatches(input,cities),html=matchArray.map(place=>{const regex=new RegExp(input,"gi"),cityName=place.city.replace(regex,`<span class="hl">${input}</span>`),stateName=place.state.replace(regex,`<span class="hl">${input}</span>`);return`\n      <li>\n        <span class="name">${cityName}, ${stateName}</span>\n        <span class="population">${place.population}</span>\n      </li>\n    `}).join("");suggestions.innerHTML=html};searchInput.addEventListener("change",displayMatches),searchInput.addEventListener("keyup",displayMatches);
//# sourceMappingURL=main.min.js.map