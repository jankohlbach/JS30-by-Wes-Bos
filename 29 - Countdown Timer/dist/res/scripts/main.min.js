const timerDisplay=document.querySelector(".display-time-left"),endTime=document.querySelector(".display-end-time"),buttons=document.querySelectorAll("[data-time]");let countdown;function displayTimeLeft(seconds){const minutes=Math.floor(seconds/60),remainderSeconds=seconds%60,display=`${minutes}:${remainderSeconds<10?"0":""}${remainderSeconds}`;document.title=display,timerDisplay.textContent=display}function displayEndTime(timestamp){const end=new Date(timestamp),hour=end.getHours(),minutes=end.getMinutes();endTime.textContent=`Be Back At ${hour}:${minutes<10?"0":""}${minutes}`}function timer(seconds){clearInterval(countdown);const now=Date.now(),then=now+1e3*seconds;displayTimeLeft(seconds),displayEndTime(then),countdown=setInterval(()=>{const secondsLeft=Math.round((then-Date.now())/1e3);secondsLeft<0?clearInterval(countdown):displayTimeLeft(secondsLeft)},1e3)}function startTimer(){const seconds=parseInt(this.dataset.time,10);timer(seconds)}buttons.forEach(button=>button.addEventListener("click",startTimer)),document.customForm.addEventListener("submit",e=>{e.preventDefault();const mins=document.customForm.minutes.value;timer(60*mins),document.customForm.reset()});
//# sourceMappingURL=main.min.js.map