const checkboxes=document.querySelectorAll("input");let lastChecked;function handleCheck(event){let inBetween=!1;event.shiftKey&&this.checked&&checkboxes.forEach(function(checkbox){checkbox!==this&&checkbox!==lastChecked||(inBetween=!inBetween),inBetween&&(checkbox.checked=!0)},this),lastChecked=this}checkboxes.forEach(function(checkbox){checkbox.addEventListener("click",handleCheck)});